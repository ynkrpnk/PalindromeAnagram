'use strict';


const separator = '';
const palindromString = (str, separator) => str.split(separator).reverse().join(separator)

const reverseLine = (str) =>{
    const separator = '';
    const strWithoutGaps = str.toLowerCase().replaseAll('', separator);

}


console.log(palindromString('mama', separator));